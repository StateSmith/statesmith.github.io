// Autogenerated with StateSmith 0.8.10-alpha.
// Algorithm: Balanced1. See https://github.com/StateSmith/StateSmith/wiki/Algorithms

// Generated state machine
class Ex01Polled
 extends Ex10Base{
    static EventId = 
    {
        DO : 0, // The `do` event is special. State event handlers do not consume this event (ancestors all get it too) unless a transition occurs.
    }
    static { Object.freeze(this.EventId); }
    
    static EventIdCount = 1;
    static { Object.freeze(this.EventIdCount); }
    
    static StateId = 
    {
        ROOT : 0,
        OFF : 1,
        ON1 : 2,
    }
    static { Object.freeze(this.StateId); }
    
    static StateIdCount = 3;
    static { Object.freeze(this.StateIdCount); }
    
    // Used internally by state machine. Feel free to inspect, but don't modify.
    stateId;
    
    // Used internally by state machine. Don't modify.
    #ancestorEventHandler;
    
    // Used internally by state machine. Don't modify.
    #currentEventHandlers = Array(Ex01Polled.EventIdCount).fill(undefined);
    
    // Used internally by state machine. Don't modify.
    #currentStateExitHandler;
    
    // Variables. Can be used for inputs, outputs, user variables...
    vars = {
        t1_start_ms: 0,
        count: 0,
        switch_is_on: false,
    };
    
    // Starts the state machine. Must be called before dispatching events. Not thread safe.
    start()
    {
        this.#ROOT_enter();
        // ROOT behavior
        // uml: TransitionTo(ROOT.InitialState)
        {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
            
            // Step 2: Transition action: ``.
            
            // Step 3: Enter/move towards transition target `ROOT.InitialState`.
            // ROOT.InitialState is a pseudo state and cannot have an `enter` trigger.
            
            // ROOT.InitialState behavior
            // uml: / { trace_transition("14"); } TransitionTo(OFF)
            {
                // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
                
                // Step 2: Transition action: `trace_transition("14");`.
                this.trace_transition("14");
                
                // Step 3: Enter/move towards transition target `OFF`.
                this.#OFF_enter();
                
                // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
                this.stateId = Ex01Polled.StateId.OFF;
                // No ancestor handles event. Can skip nulling `ancestorEventHandler`.
                return;
            } // end of behavior for ROOT.InitialState
        } // end of behavior for ROOT
    }
    
    // Dispatches an event to the state machine. Not thread safe.
    dispatchEvent(/** @type {Ex01Polled.EventId} */ eventId)
    {
        let behaviorFunc = this.#currentEventHandlers[eventId];
        
        while (behaviorFunc != null)
        {
            this.#ancestorEventHandler = null;
            behaviorFunc.call(this);
            behaviorFunc = this.#ancestorEventHandler;
        }
    }
    
    // This function is used when StateSmith doesn't know what the active leaf state is at
    // compile time due to sub states or when multiple states need to be exited.
    #exitUpToStateHandler(/** @type {Ex01Polled.Func} */ desiredStateExitHandler)
    {
        while (this.#currentStateExitHandler != desiredStateExitHandler)
        {
            this.#currentStateExitHandler.call(this);
        }
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state ROOT
    ////////////////////////////////////////////////////////////////////////////////
    
    #ROOT_enter()
    {
        // setup trigger/event handlers
        this.#currentStateExitHandler = this.#ROOT_exit;
    }
    
    #ROOT_exit()
    {
;
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state OFF
    ////////////////////////////////////////////////////////////////////////////////
    
    #OFF_enter()
    {
        // setup trigger/event handlers
        this.#currentStateExitHandler = this.#OFF_exit;
        this.#currentEventHandlers[Ex01Polled.EventId.DO] = this.#OFF_do;
        
        // OFF behavior
        // uml: enter / { trace_enter("OFF", "16"); }
        {
            // Step 1: execute action `trace_enter("OFF", "16");`
            this.trace_enter("OFF", "16");
        } // end of behavior for OFF
        
        // OFF behavior
        // uml: enter / { trace_action("light_off();", "17", "enter / { light_off(); }");light_off(); }
        {
            // Step 1: execute action `trace_action("light_off();", "17", "enter / { light_off(); }");light_off();`
            this.trace_action("light_off();", "17", "enter / { light_off(); }");this.light_off();
        } // end of behavior for OFF
    }
    
    #OFF_exit()
    {
        // OFF behavior
        // uml: exit / { trace_exit("OFF", "16"); }
        {
            // Step 1: execute action `trace_exit("OFF", "16");`
            this.trace_exit("OFF", "16");
        } // end of behavior for OFF
        
        // adjust function pointers for this state's exit
        this.#currentStateExitHandler = this.#ROOT_exit;
        this.#currentEventHandlers[Ex01Polled.EventId.DO] = null;  // no ancestor listens to this event
    }
    
    #OFF_do()
    {
        // No ancestor state handles `do` event.
        
        // OFF behavior
        // uml: do [switch_is_on] / { trace_transition("34"); } TransitionTo(ON1)
        if (this.vars.switch_is_on)
        {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
            this.#OFF_exit();
            
            // Step 2: Transition action: `trace_transition("34");`.
            this.trace_transition("34");
            
            // Step 3: Enter/move towards transition target `ON1`.
            this.#ON1_enter();
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            this.stateId = Ex01Polled.StateId.ON1;
            // No ancestor handles event. Can skip nulling `ancestorEventHandler`.
            return;
        } // end of behavior for OFF
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state ON1
    ////////////////////////////////////////////////////////////////////////////////
    
    #ON1_enter()
    {
        // setup trigger/event handlers
        this.#currentStateExitHandler = this.#ON1_exit;
        this.#currentEventHandlers[Ex01Polled.EventId.DO] = this.#ON1_do;
        
        // ON1 behavior
        // uml: enter / { trace_enter("ON1", "23"); }
        {
            // Step 1: execute action `trace_enter("ON1", "23");`
            this.trace_enter("ON1", "23");
        } // end of behavior for ON1
        
        // ON1 behavior
        // uml: enter / { trace_action("light_blue();", "24", "enter / { light_blue(); }");light_blue(); }
        {
            // Step 1: execute action `trace_action("light_blue();", "24", "enter / { light_blue(); }");light_blue();`
            this.trace_action("light_blue();", "24", "enter / { light_blue(); }");this.light_blue();
        } // end of behavior for ON1
    }
    
    #ON1_exit()
    {
        // ON1 behavior
        // uml: exit / { trace_exit("ON1", "23"); }
        {
            // Step 1: execute action `trace_exit("ON1", "23");`
            this.trace_exit("ON1", "23");
        } // end of behavior for ON1
        
        // adjust function pointers for this state's exit
        this.#currentStateExitHandler = this.#ROOT_exit;
        this.#currentEventHandlers[Ex01Polled.EventId.DO] = null;  // no ancestor listens to this event
    }
    
    #ON1_do()
    {
        // No ancestor state handles `do` event.
        
        // ON1 behavior
        // uml: do [!switch_is_on] / { trace_transition("32"); } TransitionTo(OFF)
        if (!this.vars.switch_is_on)
        {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
            this.#ON1_exit();
            
            // Step 2: Transition action: `trace_transition("32");`.
            this.trace_transition("32");
            
            // Step 3: Enter/move towards transition target `OFF`.
            this.#OFF_enter();
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            this.stateId = Ex01Polled.StateId.OFF;
            // No ancestor handles event. Can skip nulling `ancestorEventHandler`.
            return;
        } // end of behavior for ON1
    }
    
    // Thread safe.
    stateIdToString(/** @type {Ex01Polled.StateId} */ id)
    {
        switch (id)
        {
            case Ex01Polled.StateId.ROOT: return "ROOT";
            case Ex01Polled.StateId.OFF: return "OFF";
            case Ex01Polled.StateId.ON1: return "ON1";
            default: return "?";
        }
    }
    
    // Thread safe.
    eventIdToString(/** @type {Ex01Polled.EventId} */ id)
    {
        switch (id)
        {
            case Ex01Polled.EventId.DO: return "DO";
            default: return "?";
        }
    }
}
