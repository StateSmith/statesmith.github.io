// Autogenerated with StateSmith 0.8.10-alpha.
// Algorithm: Balanced1. See https://github.com/StateSmith/StateSmith/wiki/Algorithms

// Generated state machine
class Ex03
 extends Ex10Base{
    static EventId = 
    {
        DIM : 0,
        INCREASE : 1,
    }
    static { Object.freeze(this.EventId); }
    
    static EventIdCount = 2;
    static { Object.freeze(this.EventIdCount); }
    
    static StateId = 
    {
        ROOT : 0,
        OFF : 1,
        ON1 : 2,
        ON2 : 3,
        ON3 : 4,
    }
    static { Object.freeze(this.StateId); }
    
    static StateIdCount = 5;
    static { Object.freeze(this.StateIdCount); }
    
    // Used internally by state machine. Feel free to inspect, but don't modify.
    stateId;
    
    // Used internally by state machine. Don't modify.
    #ancestorEventHandler;
    
    // Used internally by state machine. Don't modify.
    #currentEventHandlers = Array(Ex03.EventIdCount).fill(undefined);
    
    // Used internally by state machine. Don't modify.
    #currentStateExitHandler;
    
    // Variables. Can be used for inputs, outputs, user variables...
    vars = {
        t1_start_ms: 0,
        count: 0,
        switch_is_on: false,
    };
    
    // Starts the state machine. Must be called before dispatching events. Not thread safe.
    start()
    {
        this.#ROOT_enter();
        // ROOT behavior
        // uml: TransitionTo(ROOT.InitialState)
        {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
            
            // Step 2: Transition action: ``.
            
            // Step 3: Enter/move towards transition target `ROOT.InitialState`.
            // ROOT.InitialState is a pseudo state and cannot have an `enter` trigger.
            
            // ROOT.InitialState behavior
            // uml: / { trace_transition("14"); } TransitionTo(OFF)
            {
                // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition). Already at LCA, no exiting required.
                
                // Step 2: Transition action: `trace_transition("14");`.
                this.trace_transition("14");
                
                // Step 3: Enter/move towards transition target `OFF`.
                this.#OFF_enter();
                
                // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
                this.stateId = Ex03.StateId.OFF;
                // No ancestor handles event. Can skip nulling `ancestorEventHandler`.
                return;
            } // end of behavior for ROOT.InitialState
        } // end of behavior for ROOT
    }
    
    // Dispatches an event to the state machine. Not thread safe.
    dispatchEvent(/** @type {Ex03.EventId} */ eventId)
    {
        let behaviorFunc = this.#currentEventHandlers[eventId];
        
        while (behaviorFunc != null)
        {
            this.#ancestorEventHandler = null;
            behaviorFunc.call(this);
            behaviorFunc = this.#ancestorEventHandler;
        }
    }
    
    // This function is used when StateSmith doesn't know what the active leaf state is at
    // compile time due to sub states or when multiple states need to be exited.
    #exitUpToStateHandler(/** @type {Ex03.Func} */ desiredStateExitHandler)
    {
        while (this.#currentStateExitHandler != desiredStateExitHandler)
        {
            this.#currentStateExitHandler.call(this);
        }
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state ROOT
    ////////////////////////////////////////////////////////////////////////////////
    
    #ROOT_enter()
    {
        // setup trigger/event handlers
        this.#currentStateExitHandler = this.#ROOT_exit;
    }
    
    #ROOT_exit()
    {
;
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state OFF
    ////////////////////////////////////////////////////////////////////////////////
    
    #OFF_enter()
    {
        // setup trigger/event handlers
        this.#currentStateExitHandler = this.#OFF_exit;
        this.#currentEventHandlers[Ex03.EventId.INCREASE] = this.#OFF_increase;
        
        // OFF behavior
        // uml: enter / { trace_enter("OFF", "16"); }
        {
            // Step 1: execute action `trace_enter("OFF", "16");`
            this.trace_enter("OFF", "16");
        } // end of behavior for OFF
        
        // OFF behavior
        // uml: enter / { trace_action("light_off();", "17", "enter / { light_off(); }");light_off(); }
        {
            // Step 1: execute action `trace_action("light_off();", "17", "enter / { light_off(); }");light_off();`
            this.trace_action("light_off();", "17", "enter / { light_off(); }");this.light_off();
        } // end of behavior for OFF
    }
    
    #OFF_exit()
    {
        // OFF behavior
        // uml: exit / { trace_exit("OFF", "16"); }
        {
            // Step 1: execute action `trace_exit("OFF", "16");`
            this.trace_exit("OFF", "16");
        } // end of behavior for OFF
        
        // adjust function pointers for this state's exit
        this.#currentStateExitHandler = this.#ROOT_exit;
        this.#currentEventHandlers[Ex03.EventId.INCREASE] = null;  // no ancestor listens to this event
    }
    
    #OFF_increase()
    {
        // No ancestor state handles `increase` event.
        
        // OFF behavior
        // uml: INCREASE / { trace_transition("37"); } TransitionTo(ON1)
        {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
            this.#OFF_exit();
            
            // Step 2: Transition action: `trace_transition("37");`.
            this.trace_transition("37");
            
            // Step 3: Enter/move towards transition target `ON1`.
            this.#ON1_enter();
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            this.stateId = Ex03.StateId.ON1;
            // No ancestor handles event. Can skip nulling `ancestorEventHandler`.
            return;
        } // end of behavior for OFF
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state ON1
    ////////////////////////////////////////////////////////////////////////////////
    
    #ON1_enter()
    {
        // setup trigger/event handlers
        this.#currentStateExitHandler = this.#ON1_exit;
        this.#currentEventHandlers[Ex03.EventId.DIM] = this.#ON1_dim;
        this.#currentEventHandlers[Ex03.EventId.INCREASE] = this.#ON1_increase;
        
        // ON1 behavior
        // uml: enter / { trace_enter("ON1", "23"); }
        {
            // Step 1: execute action `trace_enter("ON1", "23");`
            this.trace_enter("ON1", "23");
        } // end of behavior for ON1
        
        // ON1 behavior
        // uml: enter / { trace_action("light_blue();", "24", "enter / { light_blue(); }");light_blue(); }
        {
            // Step 1: execute action `trace_action("light_blue();", "24", "enter / { light_blue(); }");light_blue();`
            this.trace_action("light_blue();", "24", "enter / { light_blue(); }");this.light_blue();
        } // end of behavior for ON1
    }
    
    #ON1_exit()
    {
        // ON1 behavior
        // uml: exit / { trace_exit("ON1", "23"); }
        {
            // Step 1: execute action `trace_exit("ON1", "23");`
            this.trace_exit("ON1", "23");
        } // end of behavior for ON1
        
        // adjust function pointers for this state's exit
        this.#currentStateExitHandler = this.#ROOT_exit;
        this.#currentEventHandlers[Ex03.EventId.DIM] = null;  // no ancestor listens to this event
        this.#currentEventHandlers[Ex03.EventId.INCREASE] = null;  // no ancestor listens to this event
    }
    
    #ON1_dim()
    {
        // No ancestor state handles `dim` event.
        
        // ON1 behavior
        // uml: DIM / { trace_transition("32"); } TransitionTo(OFF)
        {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
            this.#ON1_exit();
            
            // Step 2: Transition action: `trace_transition("32");`.
            this.trace_transition("32");
            
            // Step 3: Enter/move towards transition target `OFF`.
            this.#OFF_enter();
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            this.stateId = Ex03.StateId.OFF;
            // No ancestor handles event. Can skip nulling `ancestorEventHandler`.
            return;
        } // end of behavior for ON1
    }
    
    #ON1_increase()
    {
        // No ancestor state handles `increase` event.
        
        // ON1 behavior
        // uml: INCREASE / { trace_transition("28"); } TransitionTo(ON2)
        {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
            this.#ON1_exit();
            
            // Step 2: Transition action: `trace_transition("28");`.
            this.trace_transition("28");
            
            // Step 3: Enter/move towards transition target `ON2`.
            this.#ON2_enter();
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            this.stateId = Ex03.StateId.ON2;
            // No ancestor handles event. Can skip nulling `ancestorEventHandler`.
            return;
        } // end of behavior for ON1
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state ON2
    ////////////////////////////////////////////////////////////////////////////////
    
    #ON2_enter()
    {
        // setup trigger/event handlers
        this.#currentStateExitHandler = this.#ON2_exit;
        this.#currentEventHandlers[Ex03.EventId.DIM] = this.#ON2_dim;
        this.#currentEventHandlers[Ex03.EventId.INCREASE] = this.#ON2_increase;
        
        // ON2 behavior
        // uml: enter / { trace_enter("ON2", "25"); }
        {
            // Step 1: execute action `trace_enter("ON2", "25");`
            this.trace_enter("ON2", "25");
        } // end of behavior for ON2
        
        // ON2 behavior
        // uml: enter / { trace_action("light_yellow();", "26", "enter / { light_yellow(); }");light_yellow(); }
        {
            // Step 1: execute action `trace_action("light_yellow();", "26", "enter / { light_yellow(); }");light_yellow();`
            this.trace_action("light_yellow();", "26", "enter / { light_yellow(); }");this.light_yellow();
        } // end of behavior for ON2
    }
    
    #ON2_exit()
    {
        // ON2 behavior
        // uml: exit / { trace_exit("ON2", "25"); }
        {
            // Step 1: execute action `trace_exit("ON2", "25");`
            this.trace_exit("ON2", "25");
        } // end of behavior for ON2
        
        // adjust function pointers for this state's exit
        this.#currentStateExitHandler = this.#ROOT_exit;
        this.#currentEventHandlers[Ex03.EventId.DIM] = null;  // no ancestor listens to this event
        this.#currentEventHandlers[Ex03.EventId.INCREASE] = null;  // no ancestor listens to this event
    }
    
    #ON2_dim()
    {
        // No ancestor state handles `dim` event.
        
        // ON2 behavior
        // uml: DIM / { trace_transition("27"); } TransitionTo(ON1)
        {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
            this.#ON2_exit();
            
            // Step 2: Transition action: `trace_transition("27");`.
            this.trace_transition("27");
            
            // Step 3: Enter/move towards transition target `ON1`.
            this.#ON1_enter();
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            this.stateId = Ex03.StateId.ON1;
            // No ancestor handles event. Can skip nulling `ancestorEventHandler`.
            return;
        } // end of behavior for ON2
    }
    
    #ON2_increase()
    {
        // No ancestor state handles `increase` event.
        
        // ON2 behavior
        // uml: INCREASE / { trace_transition("31"); } TransitionTo(ON3)
        {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
            this.#ON2_exit();
            
            // Step 2: Transition action: `trace_transition("31");`.
            this.trace_transition("31");
            
            // Step 3: Enter/move towards transition target `ON3`.
            this.#ON3_enter();
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            this.stateId = Ex03.StateId.ON3;
            // No ancestor handles event. Can skip nulling `ancestorEventHandler`.
            return;
        } // end of behavior for ON2
    }
    
    
    ////////////////////////////////////////////////////////////////////////////////
    // event handlers for state ON3
    ////////////////////////////////////////////////////////////////////////////////
    
    #ON3_enter()
    {
        // setup trigger/event handlers
        this.#currentStateExitHandler = this.#ON3_exit;
        this.#currentEventHandlers[Ex03.EventId.DIM] = this.#ON3_dim;
        
        // ON3 behavior
        // uml: enter / { trace_enter("ON3", "19"); }
        {
            // Step 1: execute action `trace_enter("ON3", "19");`
            this.trace_enter("ON3", "19");
        } // end of behavior for ON3
        
        // ON3 behavior
        // uml: enter / { trace_action("light_red();", "20", "enter / { light_red(); }");light_red(); }
        {
            // Step 1: execute action `trace_action("light_red();", "20", "enter / { light_red(); }");light_red();`
            this.trace_action("light_red();", "20", "enter / { light_red(); }");this.light_red();
        } // end of behavior for ON3
    }
    
    #ON3_exit()
    {
        // ON3 behavior
        // uml: exit / { trace_exit("ON3", "19"); }
        {
            // Step 1: execute action `trace_exit("ON3", "19");`
            this.trace_exit("ON3", "19");
        } // end of behavior for ON3
        
        // adjust function pointers for this state's exit
        this.#currentStateExitHandler = this.#ROOT_exit;
        this.#currentEventHandlers[Ex03.EventId.DIM] = null;  // no ancestor listens to this event
    }
    
    #ON3_dim()
    {
        // No ancestor state handles `dim` event.
        
        // ON3 behavior
        // uml: DIM / { trace_transition("39"); } TransitionTo(ON2)
        {
            // Step 1: Exit states until we reach `ROOT` state (Least Common Ancestor for transition).
            this.#ON3_exit();
            
            // Step 2: Transition action: `trace_transition("39");`.
            this.trace_transition("39");
            
            // Step 3: Enter/move towards transition target `ON2`.
            this.#ON2_enter();
            
            // Step 4: complete transition. Ends event dispatch. No other behaviors are checked.
            this.stateId = Ex03.StateId.ON2;
            // No ancestor handles event. Can skip nulling `ancestorEventHandler`.
            return;
        } // end of behavior for ON3
    }
    
    // Thread safe.
    stateIdToString(/** @type {Ex03.StateId} */ id)
    {
        switch (id)
        {
            case Ex03.StateId.ROOT: return "ROOT";
            case Ex03.StateId.OFF: return "OFF";
            case Ex03.StateId.ON1: return "ON1";
            case Ex03.StateId.ON2: return "ON2";
            case Ex03.StateId.ON3: return "ON3";
            default: return "?";
        }
    }
    
    // Thread safe.
    eventIdToString(/** @type {Ex03.EventId} */ id)
    {
        switch (id)
        {
            case Ex03.EventId.DIM: return "DIM";
            case Ex03.EventId.INCREASE: return "INCREASE";
            default: return "?";
        }
    }
}
